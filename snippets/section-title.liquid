<{{ tag | default: "h2" }} class="c-section-title c-section-title--{{ size | default: "small" }} c-section-title--{{ color | default: "default" }}" {{ attributes }}>
  {{- title -}}
</{{ tag | default: "h2" }}>
